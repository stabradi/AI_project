Requires PyBrain, SciPy, and probably some others.

Run the simulator withthis command:

python simple_sim.py

Do NOT run CC.py directly. It will not work. That program is deprecated.

simple_sim.py output format:
<TODO>